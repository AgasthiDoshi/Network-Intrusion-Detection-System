# This project has no Python package dependencies.
# `pip install -r requirement.txt` succeeds but installs nothing.
# Install JDK 17 or newer separately; java and javac must be on PATH.
# Java Swing/AWT is included with the JDK.
